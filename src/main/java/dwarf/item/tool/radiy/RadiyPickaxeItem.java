package dwarf.item.tool.radiy;

import dwarf.item.tool.base.BasedPickaxeItem;
import net.minecraft.item.ToolMaterial;

public class RadiyPickaxeItem extends BasedPickaxeItem {
    public RadiyPickaxeItem(ToolMaterial material, int attackDamage, float attackSpeed, Settings settings) {
        super(material, attackDamage, attackSpeed, settings);
    }
}
