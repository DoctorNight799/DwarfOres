package dwarf.item.tool.radiy;

import dwarf.item.tool.base.BasedSwordItem;
import net.minecraft.item.ToolMaterial;

public class RadiySwordItem extends BasedSwordItem {
    public RadiySwordItem(ToolMaterial material, int attackDamage, float attackSpeed, Settings settings) {
        super(material, attackDamage, attackSpeed, settings);
    }
}
