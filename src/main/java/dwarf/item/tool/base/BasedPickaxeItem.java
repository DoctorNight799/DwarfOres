package dwarf.item.tool.base;

import net.minecraft.item.ToolMaterial;
import ru.bclib.items.tool.BasePickaxeItem;

public class BasedPickaxeItem extends BasePickaxeItem {
    public BasedPickaxeItem(ToolMaterial material, int attackDamage, float attackSpeed, Settings settings) {
        super(material, attackDamage, attackSpeed, settings);
    }
}
